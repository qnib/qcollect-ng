ticker:
    interval: 5000
collectors: "docker-stats,file"
filters: "id"
handlers: "influxdb"
collector:
  file:
    path: "resources/metrics.graphite"
    reopen: true                  # If file is truncated it will be reopened
handler:
  log:
    inputs: "id"
  influxdb:
    server: "172.17.0.1"
    inputs: "id"
